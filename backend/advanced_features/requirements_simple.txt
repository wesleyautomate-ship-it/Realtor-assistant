# Simplified Advanced Features Requirements
# Install these AFTER the main requirements.txt

# Core ML Libraries (if not already installed)
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# NLP Libraries (if not already installed)
textblob>=0.17.1
spacy>=3.6.0

# API and Web Requests
requests>=2.31.0

# Data Visualization (optional)
matplotlib>=3.7.0
seaborn>=0.12.0

# Additional Utilities
python-dateutil>=2.8.0
pytz>=2023.3

# Note: Many dependencies are already in main requirements.txt
# This file only includes what's missing or needs specific versions
